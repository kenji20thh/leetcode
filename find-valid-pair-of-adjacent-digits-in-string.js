/**
 * @param {string} s
 * @return {string}
 */
var findValidPair = function(s) {
    let result = ""
    let count = 1
    for (let i = 0 ; i < s.length ; i++) {
        let n = 0
        n = s[i]
        if 
    }  
};